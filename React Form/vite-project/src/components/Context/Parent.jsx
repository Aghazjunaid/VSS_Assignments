import React,{useContext} from 'react'
import Child from './child'

function Parent() {
    return (
        <div>
            Parent
            <Child/>
        </div>
    )
}

export default Parent
